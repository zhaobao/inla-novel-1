export default class {

    itemName
    itemType
    itemBackground

    constructor(name, type, background) {
        this.itemName = name;
        this.itemType = type;
        this.itemBackground = background;
    }
}